#coding=utf8

'''
@author: chengjie
'''

import logging
import threading
import select
        
        
class actionManager(object):

    def __init__(self):
        '''
        Constructor
        '''
        """   
        def send(self, msg):
            
            #send data though socket handler
            
            
            #self.sendHandler = sendHandler()
            self.sendHandler.setHandler(self.sock)
            self.sendHandler.setStream(msg)
            self.sendHandler.setDaemon(False)
            self.sendHandler.start()
        """
    def exit(self):
        
        while not self.sendHandler.isAlive():
            pass
            self.sock.close()
        logging.info("robot exited.")
            

                
                
    def action_logi11111111111n(self):
        
        """
        """
        self.connectTcp(self.robot_config["server"]["login"])
        #self.sock.settimeout(self.robot_config["socket"]["timeout"])
        self.addSendSocket("123")
        logging.info("action login executed.")
        
        self.exit()
        return True, 123
    
    def action_send(self):
        self.send("234")
        return True, 123